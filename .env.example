NODE_ENV=development
PORT=3000

# PostgreSQL — local Docker
DATABASE_URL=postgresql://api:secret@localhost:5432/apidb

# JWT — change this! Must be >=32 chars
JWT_SECRET=changeme_this_must_be_at_least_32_characters_long
JWT_EXPIRES_IN=7d

BCRYPT_ROUNDS=12
CORS_ORIGIN=*
LOG_LEVEL=info
